from typing import List

def postprocess(postprocessing_options: List[str], **kwargs):
    if 'cluster' in postprocessing_options:
        pass
    if 'rescore' in postprocessing_options:
        pass
    if 'distribution' in postprocessing_options:
        pass
